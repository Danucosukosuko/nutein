# electron-builder config files

  * This is the config for the build settings

Each config file specifies
  - the correct file name for the build
  - publish info, such as generic provider URL where files are accessible
  - path to entitlements