export default function isWindowsDesktop(userAgent) {
    if (!userAgent)
        return false;
    const isDesktop = userAgent.indexOf('Windows');
    const isPhone = userAgent.indexOf('Windows Phone');
    return isDesktop > 0 && isPhone === -1;
}
